{
    //コンテナの表示名
    "name": "scrape_do",
    //プロジェクトのルート（devcontainer.jsonの2階層上）
    "context": "..",
    //Dockerfileの名称
    "dockerFile": "Dockerfile",
    //マウント先
    "workspaceMount": "type=bind,source=${localWorkspaceFolder},target=/opt/python/scrape_do"
}